prefix:
    "com.pitest.highwheel."

modules:
    Core = "core.*" , "core2.*"
    Utils = "utils.*"
    Modules = "modules.*"
    Parser = "parser.*"
rules:
    Parser -> Core -> Utils
    Utils -/-> Core
    Utils -/-> Parser
    Modules -> Core
    Modules -> Utils // -> Core
    Modules -> (Core, Utils)
    (Modules, Core) -> Utils
    // Modules -/-> Parser